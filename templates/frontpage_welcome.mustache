{{!
    @template theme_president/frontpage_welcome
    Welcome section for logged-in users - Clean & Simple Design
}}

<section class="frontpage-welcome-section py-4">
    <div class="president-container-fluid">
        <div class="row align-items-center">
            <div class="col-lg-7">
                <div class="welcome-content">
                    <h2 class="welcome-title mb-2">
                        {{greeting_time}}, {{user_firstname}}!
                    </h2>
                    <p class="welcome-subtitle text-muted mb-4">
                        Welcome back to {{site_fullname}}
                    </p>

                    <div class="quick-stats-grid">
                        <div class="stat-item">
                            <div class="stat-icon">
                                <i class="fa fa-book"></i>
                            </div>
                            <div class="stat-info">
                                <div class="stat-value">{{active_courses_count}}</div>
                                <div class="stat-label">Active Courses</div>
                            </div>
                        </div>

                        <div class="stat-item">
                            <div class="stat-icon">
                                <i class="fa fa-tasks"></i>
                            </div>
                            <div class="stat-info">
                                <div class="stat-value">{{pending_assignments}}</div>
                                <div class="stat-label">Pending Tasks</div>
                            </div>
                        </div>

                        {{#new_messages}}
                        <div class="stat-item">
                            <div class="stat-icon">
                                <i class="fa fa-envelope"></i>
                            </div>
                            <div class="stat-info">
                                <div class="stat-value">{{new_messages}}</div>
                                <div class="stat-label">New Messages</div>
                            </div>
                        </div>
                        {{/new_messages}}
                    </div>

                    <div class="welcome-actions mt-4">
                        <a href="{{dashboard_url}}" class="btn btn-primary">
                            Go to Dashboard
                        </a>
                        <a href="{{my_courses_url}}" class="btn btn-outline-secondary">
                            View My Courses
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
